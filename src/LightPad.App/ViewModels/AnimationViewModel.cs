namespace LightPad.App.ViewModels;

public sealed class AnimationViewModel : BaseViewModel
{
    public string StatusText => "Animation workflow is intentionally deferred until after the v1 tracing flow ships.";
}
